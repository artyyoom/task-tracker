databaseChangeLog:
  - changeSet:
      id: 1
      author: arttt (generated)
      changes:
        - createTable:
            tableName: users
            columns:
              - column:
                  name: id
                  type: BIGINT
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: username
                  type: VARCHAR(50)
                  constraints:
                    nullable: false
                    unique: true
              - column:
                  name: password
                  type: VARCHAR(255)
                  constraints:
                    nullable: false
              - column:
                  name: email
                  type: VARCHAR(255)
                  constraints:
                    nullable: false
                    unique: true

  - changeSet:
      id: 2
      author: arttt (generated)
      changes:
        - createTable:
            tableName: tasks
            columns:
              - column:
                  name: id
                  type: BIGINT
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: title
                  type: VARCHAR(255)
                  constraints:
                    nullable: false
              - column:
                  name: description
                  type: TEXT
              - column:
                  name: user_id
                  type: BIGINT
                  constraints:
                    nullable: false
              - column:
                  name: status
                  type: VARCHAR(50)
                  constraints:
                    nullable: false
              - column:
                  name: done_timestamp
                  type: TIMESTAMP
        - addForeignKeyConstraint:
            baseColumnNames: user_id
            baseTableName: tasks
            constraintName: fk_tasks_owner
            referencedColumnNames: id
            referencedTableName: users
            onDelete: CASCADE
  - changeSet:
      id: 3
      author: arttt (generated)
      changes:
        - sql:
            sql: INSERT INTO users (username, password, email) VALUES ('user', 'user', 'artem_ponedelko@mail.ru');
        - sql:
            sql: INSERT INTO users (username, password, email) VALUES ('admin', 'admin', 'ponedelkoa7@gmail.com');
  - changeSet:
      id: 4
      author: arttt (generated)
      changes:
        - sql:
            sql: INSERT INTO tasks (title, description, user_id, status, done_timestamp) VALUES ('Первая задача', 'Описание первой задачи', 1, 'finished', '2025-05-24 10:00:00');
        - sql:
            sql: INSERT INTO tasks (title, description, user_id, status, done_timestamp) VALUES ('Вторая задача', 'Описание второй задачи', 1, 'unfinished', '2025-05-24 10:00:00');
        - sql:
            sql: INSERT INTO tasks (title, description, user_id, status, done_timestamp) VALUES ('Третья задача', 'Описание третьей задачи', 2, 'finished', '2025-05-24 10:00:00');
